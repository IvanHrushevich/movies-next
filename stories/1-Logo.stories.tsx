import React, { FunctionComponent } from 'react';

import Logo from '../components/UI/Logo/Logo';

export default {
  title: 'Logo',
  component: Logo,
};

export const basic: FunctionComponent = () => <Logo />;
